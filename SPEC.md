# SPEC — むしとりアドベンチャー（277-mushitori-adventure）

- 作成日: 2026-08-31
- ステータス: 実装
- 配信先: GitHub Pages
- 最優先端末: PC/Mac ワイド画面（横長レイアウトの参考画像に忠実）。iPhoneは簡易対応（サイドパネルを畳む）

## 0. 元ネタ（参考画像）の批評

参考画像は「Nintendo Switchのeショップ風モックアップ」1枚絵。良い点と、そのままゲームにすると破綻する点を分けて設計する。

**そのまま採用する良い点**
- 3カラム構成（左＝アクション状態＋図鑑、中央＝ステージ、右＝朝昼夕夜のカード）
- 網を振る動作を「①ふりかぶり→②スイング→③つかまえた」の3コマで見せる演出
- 図鑑タブ（ぜんぶ/あさ/ひる/ゆう/よる）と発見数バッジ、コンプリート時のクラウン演出
- 時間帯ごとの世界観サブタイトル（しずくきらめく草原／まぶしい森のふち／ゆうやけの原っぱ／月あかりの森）
- ハート（3つ）、Y図鑑／X虫かご／+メニューのコンソール風ボタン列

**画像の矛盾・そのまま実装すると破綻する点 → 決定**
- 図鑑ヘッダーは「36/36」だが、右カラムの時間�帯カードは1枚6匹×4＝24匹分しか見せていない（数が合わない）。→ **36種で確定**し、時間帯ごと9種に統一。右カードは9種を3×3で全部見せる（画像は6枠だが省略と判断し全種表示に変更）
- 背景は写真品質のアニメ絵だが、本リポジトリの鉄則は「単一HTML・外部CDN無し・Canvas手描き」（041-mushiのLEARNINGS参照）。→ 背景は空グラデーション＋シルエット地形のCanvas描画に翻訳する（041-mushiと同系統の描画関数を再利用・拡張）
- 画像は据え置き画面1枚に虫が密集しており、広いオープンワールドではなく「区切られたステージ」に見える。→ 041-mushiの大きな1枚マップ探索ではなく、**時間帯ごとに閉じたステージ**（画面内を歩き回る）に方針変更。これにより中央ステージが参考画像の密度・构図に近づく
- ハートの用途が画像から不明（虫とりに戦闘は無い）。→ **スタミナ（元気）**に再定義。走ると減り、歩く/待機で回復。0になると走れない
- 時計は静止画で07:30固定。→ 実際に時間が流れる時計にする（1周＝約8〜10分、時間帯が自動で進む）。＋メニューに時短スキップも用意

## 1. ゲーム概要

- ジャンル: 里山むしとり（クローズドステージ探索＋net捕獲アクション）
- 一言説明: 時間帯ごとのステージで、種の習性を読んで近づき、網を振って捕まえ、図鑑を埋める
- 想定プレイ時間: 1周 8〜12分（4時間帯×約2分＋メニュー）
- 対応種数: 36種（時間帯9種×4）

## 2. 対象環境

- 静的 HTML / CSS / JS のみ、外部CDN無し、公開実体 20MB以下
- 基準解像度: 1440×860 相当のワイド3カラム。1100px未満はサイドカラムを折りたたみ、中央ステージのみ＋図鑑はオーバーレイ化
- 操作: PC＝矢印/WASDで移動、Shiftで走る、Cで忍び足、Spaceで網。タッチ＝画面下の仮想スティック＋あみボタン（1100px未満で表示）

## 3. ファイル構成

```text
277-mushitori-adventure/
  index.html
  style.css
  data.js     … 種データ・時間帯データ
  render.js   … 虫・地形・プレイヤーの描画関数（041-mushiのdrawBug系を継承拡張）
  game.js     … ループ・入力・状態機械・保存
  SPEC.md
  LEARNINGS.md
```

## 4. 画面レイアウト（3カラム、参考画像に対応）

- 左カラム: タイトルロゴ／アクションプレビュー（待機・歩く・走る・ふりかぶり・スイング・つかまえた！を実際のプレイヤー状態と同期してハイライト）／図鑑ミニパネル（タブ・星・発見数・コンプリート演出）
- 中央: ステージCanvas。オーバーレイ＝左上（時間帯アイコン＋時計）／右上（つかまえた数 X/36）／中央上部メッセージ／下部（ハート3・スティック・あみボタン・Y図鑑 X虫かご +メニュー）。捕獲演出は①→②→③の吹き出しをプレイヤー頭上に表示
- 右カラム: 朝・昼・夕・夜の4カード。見出し＋世界観サブタイトル＋9種のミニアイコン（発見済みは彩色、未発見はシルエット）。カードクリックで左の図鑑タブを切り替え
- 下部バナー（全幅）: 参考画像の「いろんな場所・時間で〜」相当の一言ヒント

## 5. コアループ

1. 時間帯ステージに立つ。スティック/矢印キーで歩く（弱=忍び足、強=走る。走行はスタミナ消費）
2. 虫の警戒距離を読んで近づく（走ると飛翔系が早期に逃げる）
3. 正面に虫を捉えたらあみを振る（①ふりかぶり0.12s→②スイング0.18s判定→③結果0.25s）
4. 命中で図鑑登録＋つかまえた数+1。外すと周囲の虫がわずかに逃げる
5. 時計が進み時間帯が変わるとステージの虫と背景が切り替わる（+メニューから時短スキップ可）
6. 36種すべて発見でクリア演出（コンプリートバッジ）

## 6. 種ごとの習性（typeで描画・挙動を共有）

| type | 例 | 動き | 警戒 |
|---|---|---|---|
| butterfly/moth | モンシロチョウ等 | 漂うように飛ぶ | 走りで逃げやすい |
| dragonfly | シオカラトンボ等 | ホバリング→ダート | 最も神経質 |
| bee | ミツバチ | 低空で花を巡回 | 中 |
| cicada | セミ | 静止、驚くと飛去 | 忍び足推奨 |
| beetle/stag/rhinoceros/jewel系 | カブトムシ等 | 遅い歩行 | 鈍い |
| hopper/cricket/mantis | バッタ等 | 静止→跳躍 | 接近で跳ぶ |
| firefly | ホタル | ゆっくり漂う発光 | 低い |

## 6.5 音声方針（意図的な仕様）

- BGMは実装しない。効果音（網・捕獲・新発見・からぶり）のみ、WebAudioで都度生成する短いビープ音
- 監査ツールの「BGMループ設定」項目は、本作がBGM非採用のため恒常的に×表示になる（見落としではなく仕様）

## 7. スタミナ（ハート3）

- 走行で毎秒消費。0になると走れず歩きのみ
- 歩く/待機で回復。忍び足は消費せず微回復

## 8. 保存

- `localStorage['mushitori-adv-book-v1']`: 種id→捕獲回数
- `localStorage['mushitori-adv-mute']`

## 9. 実装制約

- Canvas DPR上限2、外部CDN無し、`innerHTML`にユーザー入力を渡さない
- 1100px未満はサイドカラムを非表示にし、図鑑はボタンから全画面オーバーレイで開く

## 10. テスト項目

- 起動後、開始ボタンがDOM内のbuttonの先頭であること（harness互換）
- 矢印/WASDで8方向に歩ける。Shiftで走る、スタミナが減る
- 網は正面かつ射程内の1体だけ命中する
- 時間帯が自動で進み、朝→昼→夕→夜→朝で一周する
- 図鑑がlocalStorageから復元される
- 36/36でコンプリート演出が出る
- 1100px未満でサイドカラムが折りたたまれ、図鑑ボタンでオーバーレイが開く

## 11. 未確定事項

- iPhone実機検証は未実施（Playwrightでのデスクトップ動作確認のみ）
