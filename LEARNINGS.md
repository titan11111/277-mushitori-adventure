# LEARNINGS — 277-mushitori-adventure

## 2026-09-03 GitHub Pages 初回公開

- 公開前に `gh repo view titan11111/277-mushitori-adventure` がリポジトリ未存在だったため、出荷パイプラインで新規公開した。
- `_tools/game-harness.sh 277-mushitori-adventure` は RESULT PASS。証跡: `docs/harness-reports/277-mushitori-adventure-2026-09-03T12-19-55-843Z.md`（RAF 61、コンソール0、通信量 0.07MB）。
- iPhone実機Safariは未検証のまま（SPEC 11の未確定事項は公開ブロッカーではないと判断）。

## 2026-08-31 新規制作（参考画像ベース）

- 参考画像（Switch eショップ風モックアップ）は「36/36」表記と右カラムの時間帯カード（6匹×4＝24匹分）で数が矛盾していた。36種（時間帯9種×4）に統一し、右カードは9種を3×3で全部見せる形にして整合させた。
- 画像は据え置きの1枚絵で密集した虫が描かれており、041-mushiのような広いマップ探索には見えなかった。時間帯ごとに閉じた1ステージ（1100×700）を歩き回る設計に変更し、参考画像の構図密度に寄せた。
- ハートの用途が画像から不明（虫とりに戦闘は無い）。スタミナ（走ると減る・待機/歩行で回復）に転用し、実際に機能する要素にした。
- 時計は静止画で07:30固定だったが、実装では本当に時間を流す（1周＝約3.5分の実時間、DAY_LENGTH_SEC=210）ようにし、+メニューに時短スキップも用意した。
- 041-mushiのdrawBug系ベクター描画を継承・拡張（ladybeetle/caterpillar/jewelbeetle等のtypeを追加）。写真品質のアニメ絵はCanvas手描きの上限を超えるため目指さず、041-mushiと同じ「単純図形の組み合わせ」路線を踏襲。

## ハマった点

- **harnessの「開始ボタンを先頭のbuttonにする」慣習**を最初は破っていた（左カラムの図鑑閉じるボタン等がDOM順で先に来ていた）。修正: 中央カラム（開始ボタンを含む）をDOM上は最初の子要素にし、左右カラムより後ろに置いた。見た目の左→中央→右の並びは`grid-column`で固定し、DOM順とは独立させた。
- その際、**CSS Gridの自動配置（auto-placement）が想定外の動きをした**。3つの`.col`に`grid-column`だけ指定して`grid-row`を指定しなかったところ、DOM順で先に来る中央カラム（grid-column:2）が先に配置され、次のアイテム（左カラム、grid-column:1）は「配置カーソルより手前の列」に置けないため新しい行に送られてしまい、左右カラムだけが2行目に落ちる崩れたレイアウトになった。**3つとも`grid-row:1`を明示**して解決。DOM順とCSSの見た目順を分離するときは、行・列の両方を明示するのが安全という教訓。
- Playwright（cloud workspace側にプリインストール済み）でheadless検証。`chromium.launch`のexecutablePathは`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`（バージョン番号付きディレクトリ）だった。
- プレイヤーをテレポートさせて捕獲を直接検証する際、`window`に一時的なデバッグフックを生やして確認 → 検証後に必ず削除（納品コードに残さない）。

## 検証内容（Playwright、デスクトップ1500×900・iPhone 390×844）

- 開始ボタンがDOM先頭のbuttonであること（harness互換）: ✅
- 起動→開始→矢印移動→Shift走り→網振り→図鑑/虫かご/メニュー開閉→時短スキップ: コンソールエラー無し ✅
- プレイヤーを虫の正面射程内にテレポートしてSpace: 捕獲成功、NEWカード表示、つかまえた数1/36、図鑑・右カード・アクションパネルが即時反映: ✅
- 1100px未満でサイドカラムが折りたたまれ、中央ステージのみフル表示、図鑑ボタンでオーバーレイが開閉: ✅

## 未検証

- `_tools/game-harness.sh 277-mushitori-adventure` はこの端末のPlaywright用Chromiumキャッシュが未取得（`chromium_headless_shell`不在）で実行できなかった（本作固有の問題ではなく、この端末のPlaywright環境未整備によるもの。`npx playwright install`はネットワーク制限でも失敗）。
  代替として、クラウド側の同エンジン（Playwright/Chromium）で同等の動的検証を実施し、証跡を確認した: index.htmlの存在／開始ボタンがDOM先頭のbutton／起動→開始→移動→網振り→図鑑・虫かご・メニュー開閉の一連操作でコンソールエラー0件／実際に虫の正面射程内でSpaceを押して捕獲成功（NEWカード表示・つかまえた数1→2・図鑑と右カードへの反映）まで確認済み。
- iPhone実機（Safari）は未検証。Playwrightでのビューポート再現のみ。
- 24時間の周回・36種コンプリート演出は、時短スキップの組み合わせでの動作は確認したが、自然経過での全周回は未確認。

## 静的監査（_tools/audit.mjs）

- 初回: ○21 ×7 ―5（iOS適合系のuser-select/タップハイライト/ダブルタップズーム防止/safe-area/AudioContext resume()が未実装で×）
- 修正後: ○26 ×2 ―5。残る×2は「未コミットの変更がない」（コミットで解消想定）と「BGMループ設定」（本作はBGM非採用でSEのみのため恒常的に×。SPEC.md 6.5に明記した意図的な仕様）
